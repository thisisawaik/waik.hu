import { AutoFormDirective } from './auto-form.directive';

describe('AutoFormDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFormDirective();
    expect(directive).toBeTruthy();
  });
});
